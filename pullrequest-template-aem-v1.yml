parameters:
- name: pomFile  
  default: 'content/tds-site/pom.xml'

steps:
- task: Maven@4
  displayName: 'Build'
  inputs:
    mavenPomFile: ${{ parameters.pomFile }}
    goals: 'clean install'
    publishJUnitResults: false
